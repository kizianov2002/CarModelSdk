#pragma once

#include <stdint.h>

typedef struct
{
    char Name[256];
    char Mark[256];
    char Model[256];
    char Generation[256];

    char ProbabilityStr[256];

    char FileFullName[256];

} CarModelDataStruct;


typedef struct
{
    char Text[256];

} StringStruct;

